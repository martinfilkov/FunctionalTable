import React from 'react'

const SearchField = () => {
  return <form className='form__group field'>
    <input type="text" placeholder='Search a country' className='form__field' />
  </form>
}

export default SearchField